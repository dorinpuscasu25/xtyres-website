services:
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: xtyres-backend-postgres
  #   restart: unless-stopped
  #   env_file:
  #     - .env
  #   entrypoint:
  #     - sh
  #     - /usr/local/bin/postgres-entrypoint.sh
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #     - ./docker/postgres-entrypoint.sh:/usr/local/bin/postgres-entrypoint.sh:ro
  #   healthcheck:
  #     test:
  #       - CMD-SHELL
  #       - pg_isready -U "$${POSTGRES_USER:-$${DB_USERNAME:-postgres}}" -d "$${POSTGRES_DB:-$${DB_DATABASE:-xtyres}}"
  #     interval: 5s
  #     timeout: 5s
  #     retries: 20

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: xtyres-backend-app
    restart: unless-stopped
    working_dir: /var/www/html
    env_file:
      - .env
    # depends_on:
    #   postgres:
    #     condition: service_healthy
    ports:
      - '8000:8000'
      - '5173:5173'
    volumes:
      - .:/var/www/html
      - backend_vendor:/var/www/html/vendor
      - backend_node_modules:/var/www/html/node_modules
    command:
      - sh
      - docker/start.sh

volumes:
  postgres_data:
  backend_vendor:
  backend_node_modules:
